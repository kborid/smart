<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <import type="com.kborid.setting.R" />

        <variable
            name="mainViewModel"
            type="com.kborid.setting.vm.MainViewModel" />

        <variable
            name="address"
            type="com.kborid.setting.pojo.Address" />

        <variable
            name="handle"
            type="com.kborid.setting.ui.MainActivity" />

        <variable
            name="constant"
            type="com.kborid.setting.vm.constant.Constant" />
    </data>


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_name"
            bgColor="@{R.color.red}"
            fgColor="@{R.color.white}"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:onClick="@{() -> handle.doClick(address.province + ',' + address.city)}"
            android:text="@{address.p}"
            android:textSize="45sp"
            android:textStyle="bold|italic" />

        <TextView
            android:id="@+id/tv_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:onClick="@{() -> mainViewModel.onClick()}"
            android:text="@{mainViewModel.title}"
            android:textSize="60sp"
            android:textStyle="bold|italic" />

        <ImageView
            imgUrl="@{constant.imgUrl}"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:contentDescription="@string/app_name" />

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:onClick="@{() -> handle.onBtnClick()}"
            android:text="跳转" />
    </LinearLayout>
</layout>