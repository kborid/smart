<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <import type="com.kborid.setting.R" />

        <variable
            name="mainViewModel"
            type="com.kborid.setting.vm.MainViewModel" />

        <variable
            name="notificationViewModel"
            type="com.kborid.setting.ui.main.notifications.NotificationsViewModel" />

        <variable
            name="fragment"
            type="com.kborid.setting.ui.main.notifications.NotificationsFragment" />
    </data>


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical">

        <TextView
            android:id="@+id/text_notification"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="8dp"
            android:text="@{notificationViewModel.text}"
            android:textAlignment="center"
            android:textSize="20sp" />

        <TextView
            android:id="@+id/tv_name"
            bgColor="@{R.color.red}"
            fgColor="@{R.color.white}"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:onClick="@{() -> fragment.doClick()}"
            android:text="@{notificationViewModel.p}"
            android:textSize="45sp"
            android:textStyle="bold|italic" />

        <TextView
            android:id="@+id/tv_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:onClick="@{() -> mainViewModel.onClick()}"
            android:text="@{mainViewModel.title}"
            android:textSize="60sp"
            android:textStyle="bold|italic" />

        <ImageView
            imgUrl="@{notificationViewModel.imgUrl}"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:contentDescription="@string/app_name"
            android:onClick="@{() -> fragment.clickImage()}" />

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:onClick="@{() -> fragment.onBtnClick()}"
            android:text="跳转" />

    </LinearLayout>
</layout>
